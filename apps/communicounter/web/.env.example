# Optional: full WebSocket URL for production (e.g. wss://counter.example.com/ws).
# If unset, the app uses same host as the page with path /ws (use a reverse proxy).
# VITE_WS_URL=wss://your-host/ws
